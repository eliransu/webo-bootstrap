import styled from 'styled-components'
import { Statistic as AntWeboStatistics } from 'antd'

export const Statistics = styled(AntWeboStatistics)`
.ant-statistic-content-value-int {
  font-size: 14px;
}
`